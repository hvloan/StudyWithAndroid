<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:padding="25dp"
    android:layout_marginVertical="8dp"
    android:layout_marginHorizontal="8dp"
    android:background="@drawable/item_country_background">

    <com.makeramen.roundedimageview.RoundedImageView
        android:id="@+id/flagCountry"
        android:layout_width="150dp"
        android:layout_height="150dp"
        android:padding="8dp"
        android:scaleType="fitCenter"
        android:src="@mipmap/ic_launcher"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <TextView
        android:id="@+id/nameCountry"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/vietnam"
        android:textStyle="bold"
        android:textSize="18sp"
        android:maxLines="1"
        android:maxLength="12"
        android:ellipsize="end"
        android:textColor="@color/colorTextNameCountry"
        app:layout_constraintEnd_toEndOf="@+id/flagCountry"
        app:layout_constraintStart_toStartOf="@+id/flagCountry"
        app:layout_constraintTop_toBottomOf="@+id/flagCountry" />

</androidx.constraintlayout.widget.ConstraintLayout>